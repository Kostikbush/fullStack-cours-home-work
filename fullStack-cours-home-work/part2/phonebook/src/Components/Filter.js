export const Filter = ({handleInputFilter}) => {
    return (
        <div>
            <span>Filter show with</span>
            <input onChange={handleInputFilter} />
        </div>
    )
  }

  